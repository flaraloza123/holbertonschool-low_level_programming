#include "holberton.h"
#include <stdio.h>

/**
 * add - writes a function that adds two integers
 * @x: integer
 * @t: integer
 * Return: result of added integers
 */
int add(int x, int t)
{
return (x + t);
}
