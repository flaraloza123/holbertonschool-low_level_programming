#include "holberton.h"

/**
 * _abs - computes absolute value of integer
 * @n: integer
 * Return: returns n if positive, -n if negative
 */
int _abs(int n)
{
if (n > 0)
return (n);
else
return (-n);
}
